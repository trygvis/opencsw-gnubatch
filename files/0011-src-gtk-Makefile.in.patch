--- gnubatch-1.1/src/gtk/Makefile.in.orig	Mon Sep 21 00:53:06 2009
+++ gnubatch-1.1/src/gtk/Makefile.in	Mon Sep 21 00:53:28 2009
@@ -110,18 +110,18 @@
 		$(CC) -o $@ $(BGTKLDSVOBJS) $(LDFLAGS) $(LIB) $(LIBS)
 
 install:	all
-		$(INSTALL) $(USERMODES) $(USERBINS) $(BINDIR)
-		$(INSTALL) $(NOSETMODES) $(NOSETUBINS) $(BINDIR)
-		$(INSTALL) $(SUIDROOT) $(SETUROOTSYSBINS) $(INTBINDIR)
-		$(INSTALL) $(NOSETMODES) $(NOSETUSYSBINS) $(INTBINDIR)
-		$(INSTALL_DATA) $(XRES) $(HELPFILEDIR)
+		$(INSTALL) $(USERMODES) $(USERBINS) $(DESTDIR)$(BINDIR)
+		$(INSTALL) $(NOSETMODES) $(NOSETUBINS) $(DESTDIR)$(BINDIR)
+		$(INSTALL) $(SUIDROOT) $(SETUROOTSYSBINS) $(DESTDIR)$(INTBINDIR)
+		$(INSTALL) $(NOSETMODES) $(NOSETUSYSBINS) $(DESTDIR)$(INTBINDIR)
+		$(INSTALL_DATA) $(XRES) $(DESTDIR)$(HELPFILEDIR)
 
 install-strip:	all
-		$(INSTALL) -s $(USERMODES) $(USERBINS) $(BINDIR)
-		$(INSTALL) -s $(NOSETMODES) $(NOSETUBINS) $(BINDIR)
-		$(INSTALL) -s $(SUIDROOT) $(SETUROOTSYSBINS) $(INTBINDIR)
-		$(INSTALL) -s $(NOSETMODES) $(NOSETUSYSBINS) $(INTBINDIR)
-		$(INSTALL_DATA) $(XRES) $(HELPFILEDIR)
+		$(INSTALL) -s $(USERMODES) $(USERBINS) $(DESTDIR)$(BINDIR)
+		$(INSTALL) -s $(NOSETMODES) $(NOSETUBINS) $(DESTDIR)$(BINDIR)
+		$(INSTALL) -s $(SUIDROOT) $(SETUROOTSYSBINS) $(DESTDIR)$(INTBINDIR)
+		$(INSTALL) -s $(NOSETMODES) $(NOSETUSYSBINS) $(DESTDIR)$(INTBINDIR)
+		$(INSTALL_DATA) $(XRES) $(DESTDIR)$(HELPFILEDIR)
 
 clean:
 	$(RM) *.o xbtq xbtuser xbtr xbtr xfilemon *_s bgtksave bgtkldsv
